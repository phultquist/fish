    // shape.beginShape();
    // shape.vertex(100,100)
    // shape.vertex(50,100)
    // for (a = 0; a < 90; a+= 10) {
    //     let x = 50 * sin(a) + 50;
    //     let y = 50 * cos(a) + 50;
    //     shape.vertex(x,y);
    // }
    // shape.endShape(CLOSE);

    // shape.beginShape();
    // shape.vertex(100,0)
    // shape.vertex(100,50)
    // for (a = 90; a < 180; a+= 10) {
    //     let x = 50 * sin(a) + 50;
    //     let y = 50 * cos(a) + 50;
    //     shape.vertex(x,y);
    // }
    // shape.endShape(CLOSE);

    // shape.beginShape();
    // shape.vertex(0,0)
    // shape.vertex(50,0)
    // for (a = 180; a < 270; a+= 10) {
    //     let x = 50 * sin(a) + 50;
    //     let y = 50 * cos(a) + 50;
    //     shape.vertex(x,y);
    // }
    // shape.endShape(CLOSE);

    // shape.beginShape();
    // shape.vertex(0,100)
    // shape.vertex(0,50)
    // for (a = 270; a < 360; a+= 10) {
    //     let x = 50 * sin(a) + 50;
    //     let y = 50 * cos(a) + 50;
    //     shape.vertex(x,y);
    // }
    // shape.endShape(CLOSE);
